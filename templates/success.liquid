<div class="well-xs well-blueish-xs well-sm well-blueish-sm" data-session-id="{{ model.session_id }}">
  <div class="container">
    <div class="row">
      <div class="col-xs-2">
        <i class="fa fa-fw fa-3x fa-check-circle" style="color: #5cb85c"></i>
      </div>
      <div class="col-xs-10">
        <h3 class="text-center" style="margin: 0">{{ lang.success.header }}</h3>
        <p>{{ lang.success.message }} <a href="{{ session.continue_url }}">{{ lang.success.refresh }}</a>.</p>
      </div>
    </div>
  </div>
</div>
